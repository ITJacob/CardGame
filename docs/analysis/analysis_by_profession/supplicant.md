# 秘祈人（supplicant）· 技能构筑轴分析

> 数据来源：`docs/json/supplicant.skills.json`（共 **33** 张技能卡）。
> 由 `docs/tools/build_axis_analysis.py` 递归遍历全部效果节点，按「构筑轴(axis)」聚合生成。
> 生成日期：2026-09-21

## 一、职业总览：四条构筑轴一览

| 构筑轴 | 符号 | 名称 | 技能数 | 主动/被动 | 挂载状态 | 设计风格 |
|---|---|---|---:|---|---:|---|
| `shadow` | 🜁 | 阴影 | 7 | 5/2 | 0 | 进攻·控制 |
| `flesh` | 🜂 | 血肉 | 11 | 8/3 | 0 | 续航·进攻 |
| `soul` | 🜃 | 牧魂 | 5 | 3/2 | 0 | 进攻·防御 |
| `corruption` | 🜄 | 秽堕 | 10 | 8/2 | 0 | 进攻 |
| **合计** | — | — | **33** | **24/9** | **0** | — |

### 1.1 职业级：挂载状态类型分布

> 全职业共挂载 **0** 个状态（可驱散 0 / 不可驱散 0）。
| 状态类型 | 次数 | 占状态% |
|---|---:|---:|

## 二、各构筑轴详解

### 🜁 阴影（shadow）

**规模**：7 张技能（主动 5 / 被动 2）；稀有度分布：精良×3、普通×1、稀有×1、史诗×1、传说×1。

**效果原语构成**（type，全量递归，共 12 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 5 | 41.7% |
| `damage` | 1 | 8.3% |
| `modify_stat` | 2 | 16.7% |
| `modify_resource` | 1 | 8.3% |
| `spawn` | 1 | 8.3% |
| `move` | 2 | 16.7% |

**算子构成**（op，共 3 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 1 |
| `push_back` | 1 |
| `pull_forward` | 1 |

**改动的属性（modify_stat，共 2 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `gauge.rate` | 1 |
| `attack` | 1 |

**改动的资源（modify_resource，共 1 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `energy` | 1 |

**设计风格总结**：本轴偏【进攻·控制】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀；以 debuff、换位、封印、重定向与驱散干扰敌方决策与站位。
签名原语为 `mount_status`(5)、`modify_stat`(2)、`move`(2)；属性杠杆集中在 `gauge.rate`、`attack`，资源杠杆集中在 `energy`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **藏入阴影**：藏入阴影 3 tick：不可被敌方单体技能选中、受伤 ×0.85；出手或挨打即现形。


### 🜂 血肉（flesh）

**规模**：11 张技能（主动 8 / 被动 3）；稀有度分布：精良×6、稀有×2、普通×1、史诗×1、传说×1。

**效果原语构成**（type，全量递归，共 23 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 9 | 39.1% |
| `damage` | 3 | 13.0% |
| `modify_stat` | 1 | 4.3% |
| `modify_resource` | 3 | 13.0% |
| `dispel` | 2 | 8.7% |
| `spawn` | 1 | 4.3% |
| `heal` | 4 | 17.4% |

**算子构成**（op，共 4 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 3 |
| `if` | 1 |

**改动的属性（modify_stat，共 1 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `attack` | 1 |

**改动的资源（modify_resource，共 3 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `hp` | 2 |
| `energy` | 1 |

**设计风格总结**：本轴偏【续航·进攻】——以回血、护盾与再生类状态维持血线，拉长战斗生命周期；以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `mount_status`(9)、`heal`(4)、`modify_resource`(3)；属性杠杆集中在 `attack`，资源杠杆集中在 `hp`、`energy`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **替罪**：指定一名友军 4 tick：其受到的伤害全部转嫁给你；其若受到致死伤害，改由你承担这次死亡判定（可触发你的血肉不灭）。


### 🜃 牧魂（soul）

**规模**：5 张技能（主动 3 / 被动 2）；稀有度分布：稀有×3、史诗×1、传说×1。

**效果原语构成**（type，全量递归，共 11 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 5 | 45.5% |
| `damage` | 1 | 9.1% |
| `modify_stat` | 3 | 27.3% |
| `spawn` | 2 | 18.2% |

**算子构成**（op，共 6 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 4 |
| `if` | 1 |
| `repeat` | 1 |

**改动的属性（modify_stat，共 3 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `attack` | 1 |
| `hp_max` | 1 |
| `energy_max` | 1 |

**设计风格总结**：本轴偏【进攻·防御】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀；以叠甲、抬血上限与增益/架势类状态构筑防线，偏向站桩与减伤。
签名原语为 `mount_status`(5)、`modify_stat`(3)、`spawn`(2)；属性杠杆集中在 `attack`、`hp_max`、`energy_max`，资源杠杆集中在 —；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **堕落眷属**：消耗全部「被放牧的灵魂」层数，每 2 层召唤一只堕落眷属（生命 25%、攻击 30% 继承，至多 11 只）至己方队尾；眷属不占用同时驱使上限。


### 🜄 秽堕（corruption）

**规模**：10 张技能（主动 8 / 被动 2）；稀有度分布：普通×3、传说×3、史诗×2、精良×1、稀有×1。

**效果原语构成**（type，全量递归，共 21 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 10 | 47.6% |
| `damage` | 6 | 28.6% |
| `modify_stat` | 2 | 9.5% |
| `modify_resource` | 1 | 4.8% |
| `spawn` | 1 | 4.8% |
| `domain` | 1 | 4.8% |

**算子构成**（op，共 5 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 4 |
| `overlay` | 1 |

**改动的属性（modify_stat，共 2 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `attack` | 1 |
| `gauge.rate` | 1 |

**改动的资源（modify_resource，共 1 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `lost` | 1 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `mount_status`(10)、`damage`(6)、`modify_stat`(2)；属性杠杆集中在 `attack`、`gauge.rate`，资源杠杆集中在 `lost`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **黑暗海洋**：降临「黑暗海洋」4 回合：敌方造成伤害 ×0.75、行动条速率 ×0.85；己方获得隐匿（难以被单体技能选中）；「暗影/黑暗/污秽」标签卡效果 ×1.3，「光/净化」标签卡 ×0.7。自身迷失值 +1。


## 附：统计口径

- 数据源：`docs/json/supplicant.skills.json` 的 `cards[]`，共 33 张卡。
- 按 `axis` 字段分桶；`axis` 定义与符号取自文件级 `axes`。
- 效果原语/算子：**递归遍历**每张卡全部效果节点（卡面 `effects`、`statusDefs[].triggers[].effects`、区域/界域等任意嵌套），同名多次计数。
- 原语以效果节点 `type` 归类（19 类）；算子以 `op` 归类（9 类）。
- 挂载状态：统计每张卡 `statusDefs[]` 的 `category`（类型）与 `dispelable`（可驱散性）。
- 设计风格标签：由原语/属性/资源/状态类别映射到五大维度（进攻/防御/控制/运营/续航）后取占比≥25%的维度，至多 2 个组合而成。
- 生成脚本：`docs/tools/build_axis_analysis.py`（可重复运行）。