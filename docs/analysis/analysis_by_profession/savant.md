# 通识者（savant）· 技能构筑轴分析

> 数据来源：`docs/json/savant.skills.json`（共 **36** 张技能卡）。
> 由 `docs/tools/build_axis_analysis.py` 递归遍历全部效果节点，按「构筑轴(axis)」聚合生成。
> 生成日期：2026-09-23

## 一、职业总览：四条构筑轴一览

| 构筑轴 | 符号 | 名称 | 技能数 | 主动/被动 | 挂载状态 | 设计风格 |
|---|---|---|---:|---|---:|---|
| `lore` | 📘 | 通识 | 8 | 2/6 | 0 | 进攻 |
| `craft` | ⚙️ | 造物 | 14 | 10/4 | 0 | 进攻 |
| `astral` | ☄️ | 星律 | 7 | 5/2 | 0 | 进攻·运营 |
| `civilization` | 🏙️ | 文明 | 7 | 7/0 | 0 | 进攻·运营 |
| **合计** | — | — | **36** | **24/12** | **0** | — |

### 1.1 职业级：挂载状态类型分布

> 全职业共挂载 **0** 个状态（可驱散 0 / 不可驱散 0）。
| 状态类型 | 次数 | 占状态% |
|---|---:|---:|

## 二、各构筑轴详解

### 📘 通识（lore）

**规模**：8 张技能（主动 2 / 被动 6）；稀有度分布：普通×3、精良×2、史诗×2、传说×1。

**效果原语构成**（type，全量递归，共 13 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 3 | 23.1% |
| `modify_stat` | 8 | 61.5% |
| `modify_resource` | 1 | 7.7% |
| `dispel` | 1 | 7.7% |

**算子构成**（op，共 1 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 1 |

**改动的属性（modify_stat，共 8 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `energy_max` | 2 |
| `attack` | 2 |
| `resist:mental` | 1 |
| `resist:poison` | 1 |
| `energy_regen` | 1 |
| `hp_max` | 1 |

**改动的资源（modify_resource，共 1 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `energy` | 1 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `modify_stat`(8)、`mount_status`(3)、`modify_resource`(1)；属性杠杆集中在 `energy_max`、`attack`、`resist:mental`，资源杠杆集中在 `energy`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **唤醒记忆**：战斗开始时，自身能量 +8、能量上限 +5，持续整场。


### ⚙️ 造物（craft）

**规模**：14 张技能（主动 10 / 被动 4）；稀有度分布：普通×6、精良×3、稀有×3、史诗×2。

**效果原语构成**（type，全量递归，共 33 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 7 | 21.2% |
| `damage` | 6 | 18.2% |
| `modify_stat` | 6 | 18.2% |
| `modify_resource` | 7 | 21.2% |
| `dispel` | 1 | 3.0% |
| `spawn` | 5 | 15.2% |
| `heal` | 1 | 3.0% |

**算子构成**（op，共 7 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 5 |
| `repeat` | 2 |

**改动的属性（modify_stat，共 6 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `hp_max` | 2 |
| `attack` | 2 |
| `armor` | 1 |
| `resist:mental` | 1 |

**改动的资源（modify_resource，共 7 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `armor` | 3 |
| `gauge.current` | 2 |
| `energy` | 2 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `mount_status`(7)、`modify_resource`(7)、`damage`(6)；属性杠杆集中在 `hp_max`、`attack`、`armor`，资源杠杆集中在 `armor`、`gauge.current`、`energy`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **炼成**：在己方队尾炼成一具临时人偶（50% 生命），6 tick 后自行崩解。


### ☄️ 星律（astral）

**规模**：7 张技能（主动 5 / 被动 2）；稀有度分布：稀有×4、传说×2、史诗×1。

**效果原语构成**（type，全量递归，共 13 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 5 | 38.5% |
| `damage` | 2 | 15.4% |
| `modify_stat` | 4 | 30.8% |
| `modify_resource` | 2 | 15.4% |

**算子构成**（op，共 3 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 3 |

**改动的属性（modify_stat，共 4 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `gauge.rate` | 3 |
| `resist:$buildElement` | 1 |

**改动的资源（modify_resource，共 2 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `energy` | 1 |
| `armor` | 1 |

**设计风格总结**：本轴偏【进攻·运营】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀；以能量、推条（gauge）与迷失资源博弈撬动整场节奏，强调资源调度。
签名原语为 `mount_status`(5)、`modify_stat`(4)、`modify_resource`(2)；属性杠杆集中在 `gauge.rate`、`resist:$buildElement`，资源杠杆集中在 `energy`、`armor`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **规律权柄**：使全体友军获得「规律改写」5 tick：受到指定元素的伤害减半（构筑时选定元素：无焰=火 / 不蚀=毒 / 不流=冰 / 不击=雷）。


### 🏙️ 文明（civilization）

**规模**：7 张技能（主动 7 / 被动 0）；稀有度分布：史诗×4、传说×3。

**效果原语构成**（type，全量递归，共 18 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 5 | 27.8% |
| `modify_stat` | 5 | 27.8% |
| `modify_resource` | 2 | 11.1% |
| `dispel` | 1 | 5.6% |
| `spawn` | 1 | 5.6% |
| `heal` | 1 | 5.6% |
| `domain` | 2 | 11.1% |
| `drain` | 1 | 5.6% |

**算子构成**（op，共 8 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 5 |
| `if` | 1 |
| `overlay` | 2 |

**改动的属性（modify_stat，共 5 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `attack` | 2 |
| `gauge.rate` | 2 |
| `defense` | 1 |

**改动的资源（modify_resource，共 2 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `lost` | 2 |

**设计风格总结**：本轴偏【进攻·运营】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀；以能量、推条（gauge）与迷失资源博弈撬动整场节奏，强调资源调度。
签名原语为 `modify_stat`(5)、`mount_status`(5)、`domain`(2)；属性杠杆集中在 `attack`、`gauge.rate`、`defense`，资源杠杆集中在 `lost`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **文明图卷**：降临「文明图卷」界域 3 回合（可驱散）：己方每回合开始攻击 +3、防御 +3；敌方每 tick 被驱散 1 个增益状态；「文明 / 造物」标签卡效果 ×1.2。自身迷失值 +1。


## 附：统计口径

- 数据源：`docs/json/savant.skills.json` 的 `cards[]`，共 36 张卡。
- 按 `axis` 字段分桶；`axis` 定义与符号取自文件级 `axes`。
- 效果原语/算子：**递归遍历**每张卡全部效果节点（卡面 `effects`、`statusDefs[].triggers[].effects`、区域/界域等任意嵌套），同名多次计数。
- 原语以效果节点 `type` 归类（19 类）；算子以 `op` 归类（9 类）。
- 挂载状态：统计每张卡 `statusDefs[]` 的 `category`（类型）与 `dispelable`（可驱散性）。
- 设计风格标签：由原语/属性/资源/状态类别映射到五大维度（进攻/防御/控制/运营/续航）后取占比≥25%的维度，至多 2 个组合而成。
- 生成脚本：`docs/tools/build_axis_analysis.py`（可重复运行）。