# 囚犯（prisoner）· 技能构筑轴分析

> 数据来源：`docs/json/prisoner.skills.json`（共 **36** 张技能卡）。
> 由 `docs/tools/build_axis_analysis.py` 递归遍历全部效果节点，按「构筑轴(axis)」聚合生成。
> 生成日期：2026-09-20

## 一、职业总览：四条构筑轴一览

| 构筑轴 | 符号 | 名称 | 技能数 | 主动/被动 | 挂载状态 | 设计风格 |
|---|---|---|---:|---|---:|---|
| `curse` | 🜍 | 诅咒 | 8 | 8/0 | 0 | 进攻 |
| `bondage` | 🜎 | 束缚 | 7 | 2/5 | 0 | 进攻·控制 |
| `aberration` | 🜏 | 异类 | 13 | 9/4 | 0 | 进攻 |
| `item` | 🜐 | 物品 | 8 | 6/2 | 0 | 进攻 |
| **合计** | — | — | **36** | **25/11** | **0** | — |

### 1.1 职业级：挂载状态类型分布

> 全职业共挂载 **0** 个状态（可驱散 0 / 不可驱散 0）。
| 状态类型 | 次数 | 占状态% |
|---|---:|---:|

## 二、各构筑轴详解

### 🜍 诅咒（curse）

**规模**：8 张技能（主动 8 / 被动 0）；稀有度分布：史诗×4、稀有×2、传说×2。

**效果原语构成**（type，全量递归，共 16 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 13 | 81.2% |
| `damage` | 3 | 18.8% |

**算子构成**（op，共 3 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 2 |
| `if` | 1 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `mount_status`(13)、`damage`(3)；属性杠杆集中在 —，资源杠杆集中在 —；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **变形诅咒**：使一名敌人变形诅咒 3 回合：攻击减半、防御 -2、行动条速率减半，且无法使用技能（仍可普通攻击）。


### 🜎 束缚（bondage）

**规模**：7 张技能（主动 2 / 被动 5）；稀有度分布：普通×2、精良×2、史诗×2、传说×1。

**效果原语构成**（type，全量递归，共 9 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 6 | 66.7% |
| `damage` | 1 | 11.1% |
| `modify_stat` | 1 | 11.1% |
| `dispel` | 1 | 11.1% |

**算子构成**（op，共 1 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 1 |

**改动的属性（modify_stat，共 1 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `resist:mental` | 1 |

**设计风格总结**：本轴偏【进攻·控制】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀；以 debuff、换位、封印、重定向与驱散干扰敌方决策与站位。
签名原语为 `mount_status`(6)、`modify_stat`(1)、`dispel`(1)；属性杠杆集中在 `resist:mental`，资源杠杆集中在 —；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **被缚者**：战斗开始时获得「被缚者」：免疫一切位移与行动条后拨；你施加的枷锁、变形诅咒与灵性囚笼不可被驱散。


### 🜏 异类（aberration）

**规模**：13 张技能（主动 9 / 被动 4）；稀有度分布：精良×5、稀有×5、传说×2、普通×1。

**效果原语构成**（type，全量递归，共 28 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 9 | 32.1% |
| `damage` | 3 | 10.7% |
| `modify_stat` | 9 | 32.1% |
| `modify_resource` | 2 | 7.1% |
| `dispel` | 1 | 3.6% |
| `spawn` | 2 | 7.1% |
| `move` | 1 | 3.6% |
| `domain` | 1 | 3.6% |

**算子构成**（op，共 9 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 6 |
| `if` | 1 |
| `push_back` | 1 |
| `overlay` | 1 |

**改动的属性（modify_stat，共 9 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `attack` | 4 |
| `armor` | 3 |
| `resist:physical` | 2 |

**改动的资源（modify_resource，共 2 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `gauge.current` | 1 |
| `lost` | 1 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `modify_stat`(9)、`mount_status`(9)、`damage`(3)；属性杠杆集中在 `attack`、`armor`、`resist:physical`，资源杠杆集中在 `gauge.current`、`lost`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **转化**：将一名敌人转化为己方异种傀儡：阵营改判为己方、套用所选异种形态的属性模板、禁用其原有技能、落位到己方队尾，保留当前生命百分比。


### 🜐 物品（item）

**规模**：8 张技能（主动 6 / 被动 2）；稀有度分布：普通×3、稀有×2、精良×1、史诗×1、传说×1。

**效果原语构成**（type，全量递归，共 18 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 4 | 22.2% |
| `damage` | 4 | 22.2% |
| `modify_stat` | 6 | 33.3% |
| `modify_resource` | 2 | 11.1% |
| `dispel` | 1 | 5.6% |
| `spawn` | 1 | 5.6% |

**算子构成**（op，共 1 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 1 |

**改动的属性（modify_stat，共 6 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `attack` | 3 |
| `gauge.rate` | 1 |
| `hp_max` | 1 |
| `defense` | 1 |

**改动的资源（modify_resource，共 2 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `gauge.current` | 2 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `modify_stat`(6)、`damage`(4)、`mount_status`(4)；属性杠杆集中在 `attack`、`gauge.rate`、`hp_max`，资源杠杆集中在 `gauge.current`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **物品权柄**：己方所有物品类单位攻击 +3（4 tick）并立刻获得 30 点行动条；同时驱散全体敌人各 2 个增益。


## 附：统计口径

- 数据源：`docs/json/prisoner.skills.json` 的 `cards[]`，共 36 张卡。
- 按 `axis` 字段分桶；`axis` 定义与符号取自文件级 `axes`。
- 效果原语/算子：**递归遍历**每张卡全部效果节点（卡面 `effects`、`statusDefs[].triggers[].effects`、区域/界域等任意嵌套），同名多次计数。
- 原语以效果节点 `type` 归类（19 类）；算子以 `op` 归类（9 类）。
- 挂载状态：统计每张卡 `statusDefs[]` 的 `category`（类型）与 `dispelable`（可驱散性）。
- 设计风格标签：由原语/属性/资源/状态类别映射到五大维度（进攻/防御/控制/运营/续航）后取占比≥25%的维度，至多 2 个组合而成。
- 生成脚本：`docs/tools/build_axis_analysis.py`（可重复运行）。