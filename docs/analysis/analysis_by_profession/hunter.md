# 猎人（hunter）· 技能构筑轴分析

> 数据来源：`docs/json/hunter.skills.json`（共 **43** 张技能卡）。
> 由 `docs/tools/build_axis_analysis.py` 递归遍历全部效果节点，按「构筑轴(axis)」聚合生成。
> 生成日期：2026-09-20

## 一、职业总览：四条构筑轴一览

| 构筑轴 | 符号 | 名称 | 技能数 | 主动/被动 | 挂载状态 | 设计风格 |
|---|---|---|---:|---|---:|---|
| `flame` | 🔥 | 燃焚 | 8 | 7/1 | 0 | 进攻 |
| `intrigue` | 🕸 | 阴谋 | 10 | 7/3 | 0 | 进攻·防御 |
| `massing` | ⚔ | 集众 | 16 | 11/5 | 0 | 进攻 |
| `harvest` | 🏹 | 收割 | 9 | 5/4 | 0 | 进攻 |
| **合计** | — | — | **43** | **30/13** | **0** | — |

### 1.1 职业级：挂载状态类型分布

> 全职业共挂载 **0** 个状态（可驱散 0 / 不可驱散 0）。
| 状态类型 | 次数 | 占状态% |
|---|---:|---:|

## 二、各构筑轴详解

### 🔥 燃焚（flame）

**规模**：8 张技能（主动 7 / 被动 1）；稀有度分布：精良×7、史诗×1。

**效果原语构成**（type，全量递归，共 17 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 8 | 47.1% |
| `damage` | 6 | 35.3% |
| `modify_stat` | 1 | 5.9% |
| `modify_resource` | 1 | 5.9% |
| `move` | 1 | 5.9% |

**算子构成**（op，共 6 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 4 |
| `if` | 1 |
| `pull_forward` | 1 |

**改动的属性（modify_stat，共 1 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `resist:fire` | 1 |

**改动的资源（modify_resource，共 1 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `gauge.current` | 1 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `mount_status`(8)、`damage`(6)、`modify_resource`(1)；属性杠杆集中在 `resist:fire`，资源杠杆集中在 `gauge.current`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **紫色火焰**：自身获得紫火（整场，不可驱散）：你造成的所有火焰伤害 ×1.3，你施加的燃烧时长 +1——全途径 11 个火系技能同时升档。


### 🕸 阴谋（intrigue）

**规模**：10 张技能（主动 7 / 被动 3）；稀有度分布：普通×3、精良×3、稀有×2、史诗×2。

**效果原语构成**（type，全量递归，共 16 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 13 | 81.2% |
| `damage` | 1 | 6.2% |
| `modify_stat` | 2 | 12.5% |

**算子构成**（op，共 1 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 1 |

**改动的属性（modify_stat，共 2 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `attack` | 1 |
| `defense` | 1 |

**设计风格总结**：本轴偏【进攻·防御】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀；以叠甲、抬血上限与增益/架势类状态构筑防线，偏向站桩与减伤。
签名原语为 `mount_status`(13)、`modify_stat`(2)、`damage`(1)；属性杠杆集中在 `attack`、`defense`，资源杠杆集中在 —；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **误导**：单体敌人被误导（3 tick，1 次）：它的下一次攻击打向空位（目标改写 → first_empty）。


### ⚔ 集众（massing）

**规模**：16 张技能（主动 11 / 被动 5）；稀有度分布：史诗×7、稀有×5、传说×4。

**效果原语构成**（type，全量递归，共 36 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 13 | 36.1% |
| `damage` | 7 | 19.4% |
| `modify_stat` | 6 | 16.7% |
| `modify_resource` | 5 | 13.9% |
| `dispel` | 1 | 2.8% |
| `spawn` | 2 | 5.6% |
| `domain` | 2 | 5.6% |

**算子构成**（op，共 12 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 7 |
| `if` | 2 |
| `repeat` | 1 |
| `overlay` | 2 |

**改动的属性（modify_stat，共 6 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `attack` | 4 |
| `gauge.rate` | 2 |

**改动的资源（modify_resource，共 5 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `gauge.current` | 2 |
| `lost` | 2 |
| `shield` | 1 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `mount_status`(13)、`damage`(7)、`modify_stat`(6)；属性杠杆集中在 `attack`、`gauge.rate`，资源杠杆集中在 `gauge.current`、`lost`、`shield`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **战争权柄**：全体敌人陷入战争狂热（6 tick）：攻击最近的任何单位（不分阵营，敌我不分）、攻击 +3、护甲 -5；男性目标受伤 ×1.15（男性权柄）。


### 🏹 收割（harvest）

**规模**：9 张技能（主动 5 / 被动 4）；稀有度分布：普通×4、稀有×3、传说×2。

**效果原语构成**（type，全量递归，共 17 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 4 | 23.5% |
| `damage` | 8 | 47.1% |
| `modify_stat` | 3 | 17.6% |
| `dispel` | 1 | 5.9% |
| `drain` | 1 | 5.9% |

**算子构成**（op，共 9 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 5 |
| `if` | 3 |
| `repeat` | 1 |

**改动的属性（modify_stat，共 3 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `hp_max` | 1 |
| `attack` | 1 |
| `gauge.rate` | 1 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `damage`(8)、`mount_status`(4)、`modify_stat`(3)；属性杠杆集中在 `hp_max`、`attack`、`gauge.rate`，资源杠杆集中在 —；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **毁灭权柄**：全体敌人：8 点火焰伤害；生命值 ≤ 30% 的目标直接抹除（毁灭的彻底性：不是打伤，是不存在了）。


## 附：统计口径

- 数据源：`docs/json/hunter.skills.json` 的 `cards[]`，共 43 张卡。
- 按 `axis` 字段分桶；`axis` 定义与符号取自文件级 `axes`。
- 效果原语/算子：**递归遍历**每张卡全部效果节点（卡面 `effects`、`statusDefs[].triggers[].effects`、区域/界域等任意嵌套），同名多次计数。
- 原语以效果节点 `type` 归类（19 类）；算子以 `op` 归类（9 类）。
- 挂载状态：统计每张卡 `statusDefs[]` 的 `category`（类型）与 `dispelable`（可驱散性）。
- 设计风格标签：由原语/属性/资源/状态类别映射到五大维度（进攻/防御/控制/运营/续航）后取占比≥25%的维度，至多 2 个组合而成。
- 生成脚本：`docs/tools/build_axis_analysis.py`（可重复运行）。