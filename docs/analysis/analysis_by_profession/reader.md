# 阅读者（reader）· 技能构筑轴分析

> 数据来源：`docs/json/reader.skills.json`（共 **34** 张技能卡）。
> 由 `docs/tools/build_axis_analysis.py` 递归遍历全部效果节点，按「构筑轴(axis)」聚合生成。
> 生成日期：2026-09-19

## 一、职业总览：四条构筑轴一览

| 构筑轴 | 符号 | 名称 | 技能数 | 主动/被动 | 挂载状态 | 设计风格 |
|---|---|---|---:|---|---:|---|
| `analysis` | 🜁 | 解析 | 9 | 4/5 | 0 | 进攻·防御 |
| `arcana` | 🜂 | 秘术 | 9 | 6/3 | 0 | 进攻 |
| `mimic` | 🜃 | 模仿 | 9 | 7/2 | 0 | 进攻 |
| `foresight` | 🜄 | 预知 | 7 | 5/2 | 0 | 运营 |
| **合计** | — | — | **34** | **22/12** | **0** | — |

### 1.1 职业级：挂载状态类型分布

> 全职业共挂载 **0** 个状态（可驱散 0 / 不可驱散 0）。
| 状态类型 | 次数 | 占状态% |
|---|---:|---:|

## 二、各构筑轴详解

### 🜁 解析（analysis）

**规模**：9 张技能（主动 4 / 被动 5）；稀有度分布：普通×3、精良×3、传说×2、史诗×1。

**效果原语构成**（type，全量递归，共 17 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 6 | 35.3% |
| `damage` | 2 | 11.8% |
| `modify_stat` | 4 | 23.5% |
| `modify_resource` | 1 | 5.9% |
| `dispel` | 2 | 11.8% |
| `modify_damage` | 1 | 5.9% |
| `echo_last_skill` | 1 | 5.9% |

**算子构成**（op，共 5 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 4 |
| `if` | 1 |

**改动的属性（modify_stat，共 4 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `defense` | 2 |
| `energy_max` | 1 |
| `energy_regen` | 1 |

**改动的资源（modify_resource，共 1 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `energy` | 1 |

**设计风格总结**：本轴偏【进攻·防御】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀；以叠甲、抬血上限与增益/架势类状态构筑防线，偏向站桩与减伤。
签名原语为 `mount_status`(6)、`modify_stat`(4)、`damage`(2)；属性杠杆集中在 `defense`、`energy_max`、`energy_regen`，资源杠杆集中在 `energy`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **白塔·全知**：战斗开始时获得「白塔全知」：全体敌人视同已被解析，你每次造成伤害刷新被命中目标的解析快照，你对敌方伤害 ×1.15，持续整场。


### 🜂 秘术（arcana）

**规模**：9 张技能（主动 6 / 被动 3）；稀有度分布：精良×3、稀有×3、普通×2、传说×1。

**效果原语构成**（type，全量递归，共 20 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 6 | 30.0% |
| `damage` | 10 | 50.0% |
| `modify_stat` | 1 | 5.0% |
| `modify_resource` | 2 | 10.0% |
| `spawn` | 1 | 5.0% |

**算子构成**（op，共 3 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 3 |

**改动的属性（modify_stat，共 1 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `attack` | 1 |

**改动的资源（modify_resource，共 2 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `gauge.current` | 1 |
| `energy` | 1 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `damage`(10)、`mount_status`(6)、`modify_resource`(2)；属性杠杆集中在 `attack`，资源杠杆集中在 `gauge.current`、`energy`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **秘术·自创**：释放你自创的法术（获得时三选一）：锋锐——7 点精神伤害；守护——3 点精神伤害并为自身与相邻友军套 5 点护盾（3 tick）；洞察——3 点精神伤害、易伤 4 tick 并解析目标。


### 🜃 模仿（mimic）

**规模**：9 张技能（主动 7 / 被动 2）；稀有度分布：稀有×3、普通×2、传说×2、精良×1、史诗×1。

**效果原语构成**（type，全量递归，共 17 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 10 | 58.8% |
| `damage` | 5 | 29.4% |
| `modify_stat` | 1 | 5.9% |
| `modify_resource` | 1 | 5.9% |

**算子构成**（op，共 1 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 1 |

**改动的属性（modify_stat，共 1 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `attack` | 1 |

**改动的资源（modify_resource，共 1 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `energy` | 1 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `mount_status`(10)、`damage`(5)、`modify_resource`(1)；属性杠杆集中在 `attack`，资源杠杆集中在 `energy`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **星界导师**：使一名敌人攻击 -3（6 tick），并修改其行为概念 6 tick：接下来 2 次攻击打向空处。


### 🜄 预知（foresight）

**规模**：7 张技能（主动 5 / 被动 2）；稀有度分布：史诗×4、稀有×2、传说×1。

**效果原语构成**（type，全量递归，共 15 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 6 | 40.0% |
| `modify_stat` | 2 | 13.3% |
| `modify_resource` | 5 | 33.3% |
| `move` | 1 | 6.7% |
| `domain` | 1 | 6.7% |

**算子构成**（op，共 7 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 5 |
| `push_back` | 1 |
| `overlay` | 1 |

**改动的属性（modify_stat，共 2 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `attack` | 1 |
| `gauge.rate` | 1 |

**改动的资源（modify_resource，共 5 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `gauge.current` | 4 |
| `lost` | 1 |

**设计风格总结**：本轴偏【运营】——以能量、推条（gauge）与迷失资源博弈撬动整场节奏，强调资源调度。
签名原语为 `mount_status`(6)、`modify_resource`(5)、`modify_stat`(2)；属性杠杆集中在 `attack`、`gauge.rate`，资源杠杆集中在 `gauge.current`、`lost`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **智慧权柄**：战斗开始时获得「智慧」：你的攻击与技能必定命中、伤害 ×1.15，持续整场；自身行动条速率 +8，持续整场。


## 附：统计口径

- 数据源：`docs/json/reader.skills.json` 的 `cards[]`，共 34 张卡。
- 按 `axis` 字段分桶；`axis` 定义与符号取自文件级 `axes`。
- 效果原语/算子：**递归遍历**每张卡全部效果节点（卡面 `effects`、`statusDefs[].triggers[].effects`、区域/界域等任意嵌套），同名多次计数。
- 原语以效果节点 `type` 归类（19 类）；算子以 `op` 归类（9 类）。
- 挂载状态：统计每张卡 `statusDefs[]` 的 `category`（类型）与 `dispelable`（可驱散性）。
- 设计风格标签：由原语/属性/资源/状态类别映射到五大维度（进攻/防御/控制/运营/续航）后取占比≥25%的维度，至多 2 个组合而成。
- 生成脚本：`docs/tools/build_axis_analysis.py`（可重复运行）。