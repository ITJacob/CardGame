# 怪物（monster）· 技能构筑轴分析

> 数据来源：`docs/json/monster.skills.json`（共 **35** 张技能卡）。
> 由 `docs/tools/build_axis_analysis.py` 递归遍历全部效果节点，按「构筑轴(axis)」聚合生成。
> 生成日期：2026-09-21

## 一、职业总览：四条构筑轴一览

| 构筑轴 | 符号 | 名称 | 技能数 | 主动/被动 | 挂载状态 | 设计风格 |
|---|---|---|---:|---|---:|---|
| `inspiration` | 🜁 | 灵感 | 13 | 4/9 | 0 | 运营·进攻 |
| `fortune` | 🜂 | 幸运 | 6 | 3/3 | 0 | 综合 |
| `calamity` | 🜃 | 灾厄 | 6 | 5/1 | 0 | 进攻 |
| `cycle` | 🜄 | 循环 | 10 | 7/3 | 0 | 控制·运营 |
| **合计** | — | — | **35** | **19/16** | **0** | — |

### 1.1 职业级：挂载状态类型分布

> 全职业共挂载 **0** 个状态（可驱散 0 / 不可驱散 0）。
| 状态类型 | 次数 | 占状态% |
|---|---:|---:|

## 二、各构筑轴详解

### 🜁 灵感（inspiration）

**规模**：13 张技能（主动 4 / 被动 9）；稀有度分布：普通×7、史诗×3、稀有×2、传说×1。

**效果原语构成**（type，全量递归，共 20 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 10 | 50.0% |
| `damage` | 2 | 10.0% |
| `modify_stat` | 2 | 10.0% |
| `modify_resource` | 6 | 30.0% |

**算子构成**（op，共 4 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 2 |
| `if` | 2 |

**改动的属性（modify_stat，共 2 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `attack` | 1 |
| `defense` | 1 |

**改动的资源（modify_resource，共 6 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `gauge.current` | 4 |
| `energy` | 1 |
| `armor` | 1 |

**设计风格总结**：本轴偏【运营·进攻】——以能量、推条（gauge）与迷失资源博弈撬动整场节奏，强调资源调度；以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `mount_status`(10)、`modify_resource`(6)、`damage`(2)；属性杠杆集中在 `attack`、`defense`，资源杠杆集中在 `gauge.current`、`energy`、`armor`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **命运之轮**：双目标：互换一名敌人与一名友军的行动条进度，自身 +3 层幸运（既是好运也是厄运）。


### 🜂 幸运（fortune）

**规模**：6 张技能（主动 3 / 被动 3）；稀有度分布：精良×3、稀有×2、史诗×1。

**效果原语构成**（type，全量递归，共 10 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 10 | 100.0% |

**算子构成**（op，共 3 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 1 |
| `if` | 2 |

**设计风格总结**：本轴偏【综合】——各维度得分均不足 25% 或无有效计分原语，属分布均衡/未成形的混合型构筑轴。
签名原语为 `mount_status`(10)；属性杠杆集中在 —，资源杠杆集中在 —；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **福祸之言**：全体敌方叠 1 层厄运（4 tick），全体友军各 +1 层幸运。


### 🜃 灾厄（calamity）

**规模**：6 张技能（主动 5 / 被动 1）；稀有度分布：精良×3、稀有×3。

**效果原语构成**（type，全量递归，共 14 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 7 | 50.0% |
| `damage` | 5 | 35.7% |
| `modify_stat` | 1 | 7.1% |
| `spawn` | 1 | 7.1% |

**算子构成**（op，共 1 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 1 |

**改动的属性（modify_stat，共 1 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `resist:mental` | 1 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `mount_status`(7)、`damage`(5)、`spawn`(1)；属性杠杆集中在 `resist:mental`，资源杠杆集中在 —；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **厄运领域**：在敌方半场生成「厄运领域」，持续 8 tick：区域内敌方单位每 2 tick 叠 1 层厄运（叠满 3 层即打空并清空）。


### 🜄 循环（cycle）

**规模**：10 张技能（主动 7 / 被动 3）；稀有度分布：传说×6、史诗×3、稀有×1。

**效果原语构成**（type，全量递归，共 18 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 10 | 55.6% |
| `damage` | 1 | 5.6% |
| `modify_resource` | 3 | 16.7% |
| `dispel` | 3 | 16.7% |
| `heal` | 1 | 5.6% |

**算子构成**（op，共 3 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 2 |
| `if` | 1 |

**改动的资源（modify_resource，共 3 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `gauge.current` | 3 |

**设计风格总结**：本轴偏【控制·运营】——以 debuff、换位、封印、重定向与驱散干扰敌方决策与站位；以能量、推条（gauge）与迷失资源博弈撬动整场节奏，强调资源调度。
签名原语为 `mount_status`(10)、`modify_resource`(3)、`dispel`(3)；属性杠杆集中在 —，资源杠杆集中在 `gauge.current`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **概率权柄**：战斗开始时获得「概率权柄」（整场）：每 tick 对当前行动条最高的敌方单位判定一次——50% 概率使其行动条 -8（期望 -4/tick）。


## 附：统计口径

- 数据源：`docs/json/monster.skills.json` 的 `cards[]`，共 35 张卡。
- 按 `axis` 字段分桶；`axis` 定义与符号取自文件级 `axes`。
- 效果原语/算子：**递归遍历**每张卡全部效果节点（卡面 `effects`、`statusDefs[].triggers[].effects`、区域/界域等任意嵌套），同名多次计数。
- 原语以效果节点 `type` 归类（19 类）；算子以 `op` 归类（9 类）。
- 挂载状态：统计每张卡 `statusDefs[]` 的 `category`（类型）与 `dispelable`（可驱散性）。
- 设计风格标签：由原语/属性/资源/状态类别映射到五大维度（进攻/防御/控制/运营/续航）后取占比≥25%的维度，至多 2 个组合而成。
- 生成脚本：`docs/tools/build_axis_analysis.py`（可重复运行）。