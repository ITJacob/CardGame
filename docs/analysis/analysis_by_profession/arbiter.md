# 仲裁人（arbiter）· 技能构筑轴分析

> 数据来源：`docs/json/arbiter.skills.json`（共 **40** 张技能卡）。
> 由 `docs/tools/build_axis_analysis.py` 递归遍历全部效果节点，按「构筑轴(axis)」聚合生成。
> 生成日期：2026-09-19

## 一、职业总览：四条构筑轴一览

| 构筑轴 | 符号 | 名称 | 技能数 | 主动/被动 | 挂载状态 | 设计风格 |
|---|---|---|---:|---|---:|---|
| `jurisdiction` | 🜁 | 辖区 | 8 | 5/3 | 0 | 进攻·运营 |
| `sanction` | 🜂 | 惩戒 | 9 | 8/1 | 0 | 进攻 |
| `inquest` | 🜃 | 审讯 | 9 | 5/4 | 0 | 进攻·控制 |
| `decree` | 🜄 | 律令 | 14 | 13/1 | 0 | 进攻 |
| **合计** | — | — | **40** | **31/9** | **0** | — |

### 1.1 职业级：挂载状态类型分布

> 全职业共挂载 **0** 个状态（可驱散 0 / 不可驱散 0）。
| 状态类型 | 次数 | 占状态% |
|---|---:|---:|

## 二、各构筑轴详解

### 🜁 辖区（jurisdiction）

**规模**：8 张技能（主动 5 / 被动 3）；稀有度分布：普通×2、稀有×2、史诗×2、精良×1、传说×1。

**效果原语构成**（type，全量递归，共 15 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 6 | 40.0% |
| `damage` | 1 | 6.7% |
| `modify_stat` | 4 | 26.7% |
| `modify_resource` | 2 | 13.3% |
| `spawn` | 1 | 6.7% |
| `move` | 1 | 6.7% |

**算子构成**（op，共 3 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 2 |
| `pull_forward` | 1 |

**改动的属性（modify_stat，共 4 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `attack` | 3 |
| `gauge.rate` | 1 |

**改动的资源（modify_resource，共 2 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `gauge.current` | 2 |

**设计风格总结**：本轴偏【进攻·运营】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀；以能量、推条（gauge）与迷失资源博弈撬动整场节奏，强调资源调度。
签名原语为 `mount_status`(6)、`modify_stat`(4)、`modify_resource`(2)；属性杠杆集中在 `attack`、`gauge.rate`，资源杠杆集中在 `gauge.current`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **秩序领域**：对敌方全体立规 10 tick（可惩罚 3 次，规则可选：禁技能/禁攻击/禁加持）：每次违背受到 4 点精神伤害、被束缚 2 tick、行动条 -20。


### 🜂 惩戒（sanction）

**规模**：9 张技能（主动 8 / 被动 1）；稀有度分布：精良×3、稀有×2、史诗×2、普通×1、传说×1。

**效果原语构成**（type，全量递归，共 21 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 3 | 14.3% |
| `damage` | 15 | 71.4% |
| `modify_stat` | 1 | 4.8% |
| `modify_resource` | 1 | 4.8% |
| `dispel` | 1 | 4.8% |

**算子构成**（op，共 7 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 3 |
| `if` | 3 |
| `repeat` | 1 |

**改动的属性（modify_stat，共 1 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `attack` | 1 |

**改动的资源（modify_resource，共 1 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `gauge.current` | 1 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `damage`(15)、`mount_status`(3)、`modify_resource`(1)；属性杠杆集中在 `attack`，资源杠杆集中在 `gauge.current`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **审判**：驱散一名敌人全部增益并造成 4 点精神伤害；其身上每有一道减益/持续伤害/控制状态，追加 2 点精神伤害（最多 5 段）；其余每个敌人受到 2 点精神伤害。


### 🜃 审讯（inquest）

**规模**：9 张技能（主动 5 / 被动 4）；稀有度分布：普通×3、精良×2、史诗×2、传说×2。

**效果原语构成**（type，全量递归，共 14 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 8 | 57.1% |
| `damage` | 3 | 21.4% |
| `modify_stat` | 1 | 7.1% |
| `dispel` | 2 | 14.3% |

**算子构成**（op，共 2 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 2 |

**改动的属性（modify_stat，共 1 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `rank` | 1 |

**设计风格总结**：本轴偏【进攻·控制】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀；以 debuff、换位、封印、重定向与驱散干扰敌方决策与站位。
签名原语为 `mount_status`(8)、`damage`(3)、`dispel`(2)；属性杠杆集中在 `rank`，资源杠杆集中在 —；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **精神刺穿**：对一名近战敌人造成 4 点精神伤害，无视其护甲与护盾。


### 🜄 律令（decree）

**规模**：14 张技能（主动 13 / 被动 1）；稀有度分布：稀有×5、传说×4、精良×3、史诗×2。

**效果原语构成**（type，全量递归，共 40 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 12 | 30.0% |
| `damage` | 5 | 12.5% |
| `modify_stat` | 11 | 27.5% |
| `modify_resource` | 4 | 10.0% |
| `dispel` | 4 | 10.0% |
| `move` | 1 | 2.5% |
| `domain` | 3 | 7.5% |

**算子构成**（op，共 15 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 11 |
| `push_back` | 1 |
| `overlay` | 3 |

**改动的属性（modify_stat，共 11 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `attack` | 4 |
| `defense` | 3 |
| `armor` | 2 |
| `gauge.rate` | 2 |

**改动的资源（modify_resource，共 4 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `lost` | 3 |
| `armor` | 1 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `mount_status`(12)、`modify_stat`(11)、`damage`(5)；属性杠杆集中在 `attack`、`defense`、`armor`，资源杠杆集中在 `lost`、`armor`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **底层规则**：降临「底层规则」界域 8 tick：全场治疗量 ×2、护盾不再吸收伤害（敌我同改）；己方全体获得豁免（免疫减益类状态）8 tick。自身迷失值 +1。


## 附：统计口径

- 数据源：`docs/json/arbiter.skills.json` 的 `cards[]`，共 40 张卡。
- 按 `axis` 字段分桶；`axis` 定义与符号取自文件级 `axes`。
- 效果原语/算子：**递归遍历**每张卡全部效果节点（卡面 `effects`、`statusDefs[].triggers[].effects`、区域/界域等任意嵌套），同名多次计数。
- 原语以效果节点 `type` 归类（19 类）；算子以 `op` 归类（9 类）。
- 挂载状态：统计每张卡 `statusDefs[]` 的 `category`（类型）与 `dispelable`（可驱散性）。
- 设计风格标签：由原语/属性/资源/状态类别映射到五大维度（进攻/防御/控制/运营/续航）后取占比≥25%的维度，至多 2 个组合而成。
- 生成脚本：`docs/tools/build_axis_analysis.py`（可重复运行）。