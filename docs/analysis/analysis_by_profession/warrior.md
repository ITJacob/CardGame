# 战士（warrior）· 技能构筑轴分析

> 数据来源：`docs/json/warrior.skills.json`（共 **33** 张技能卡）。
> 由 `docs/tools/build_axis_analysis.py` 递归遍历全部效果节点，按「构筑轴(axis)」聚合生成。
> 生成日期：2026-09-23

## 一、职业总览：四条构筑轴一览

| 构筑轴 | 符号 | 名称 | 技能数 | 主动/被动 | 挂载状态 | 设计风格 |
|---|---|---|---:|---|---:|---|
| `martial` | 🗡️ | 武艺 | 15 | 10/5 | 0 | 进攻 |
| `guard` | 🛡️ | 守护 | 7 | 5/2 | 0 | 控制 |
| `hunt` | 🎯 | 猎魔 | 5 | 4/1 | 0 | 进攻 |
| `twilight` | 🌇 | 黄昏 | 6 | 3/3 | 0 | 进攻 |
| **合计** | — | — | **33** | **22/11** | **0** | — |

### 1.1 职业级：挂载状态类型分布

> 全职业共挂载 **0** 个状态（可驱散 0 / 不可驱散 0）。
| 状态类型 | 次数 | 占状态% |
|---|---:|---:|

## 二、各构筑轴详解

### 🗡️ 武艺（martial）

**规模**：15 张技能（主动 10 / 被动 5）；稀有度分布：精良×5、普通×4、史诗×3、稀有×2、传说×1。

**效果原语构成**（type，全量递归，共 33 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 8 | 24.2% |
| `damage` | 11 | 33.3% |
| `modify_stat` | 6 | 18.2% |
| `modify_resource` | 7 | 21.2% |
| `heal` | 1 | 3.0% |

**算子构成**（op，共 7 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 4 |
| `repeat` | 3 |

**改动的属性（modify_stat，共 6 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `attack` | 3 |
| `gauge.rate` | 3 |

**改动的资源（modify_resource，共 7 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `armor` | 4 |
| `gauge.current` | 3 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `damage`(11)、`mount_status`(8)、`modify_resource`(7)；属性杠杆集中在 `attack`、`gauge.rate`，资源杠杆集中在 `armor`、`gauge.current`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **战斗权柄**：战斗开始时获得「战斗权柄」：攻击 +5、护甲 +8、行动条速率 +4、全元素抗性 +10%，持续整场，不可驱散。


### 🛡️ 守护（guard）

**规模**：7 张技能（主动 5 / 被动 2）；稀有度分布：普通×2、稀有×2、史诗×2、精良×1。

**效果原语构成**（type，全量递归，共 14 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 9 | 64.3% |
| `damage` | 1 | 7.1% |
| `modify_stat` | 1 | 7.1% |
| `dispel` | 2 | 14.3% |
| `move` | 1 | 7.1% |

**算子构成**（op，共 5 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 4 |
| `pull_forward` | 1 |

**改动的属性（modify_stat，共 1 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `armor` | 1 |

**设计风格总结**：本轴偏【控制】——以 debuff、换位、封印、重定向与驱散干扰敌方决策与站位。
签名原语为 `mount_status`(9)、`dispel`(2)、`modify_stat`(1)；属性杠杆集中在 `armor`，资源杠杆集中在 —；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **水银化**：化作银液前压至己方最前排，对一名敌人造成 4 点伤害，并使其被银液淹没 3 tick（行动迟缓）。


### 🎯 猎魔（hunt）

**规模**：5 张技能（主动 4 / 被动 1）；稀有度分布：稀有×3、精良×1、史诗×1。

**效果原语构成**（type，全量递归，共 17 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 6 | 35.3% |
| `damage` | 3 | 17.6% |
| `modify_stat` | 3 | 17.6% |
| `modify_resource` | 2 | 11.8% |
| `dispel` | 1 | 5.9% |
| `heal` | 1 | 5.9% |
| `domain` | 1 | 5.9% |

**算子构成**（op，共 4 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 2 |
| `if` | 1 |
| `overlay` | 1 |

**改动的属性（modify_stat，共 3 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `attack` | 2 |
| `resist:*` | 1 |

**改动的资源（modify_resource，共 2 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `lost` | 1 |
| `gauge.current` | 1 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `mount_status`(6)、`modify_stat`(3)、`damage`(3)；属性杠杆集中在 `attack`、`resist:*`，资源杠杆集中在 `lost`、`gauge.current`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **猎魔药剂**：为一名友军涂药 4 tick：其每次造成伤害时附加 3 点毒素伤害。变体——圣膏：改为治疗 5 点并全抗性 +10%（2 tick）；精油：额外造成 2 点毒素伤害并使目标行动条 -15；印记：改为标记一名敌人，使其受伤 ×1.25（4 tick，不可驱散）。


### 🌇 黄昏（twilight）

**规模**：6 张技能（主动 3 / 被动 3）；稀有度分布：传说×4、史诗×2。

**效果原语构成**（type，全量递归，共 11 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 5 | 45.5% |
| `damage` | 3 | 27.3% |
| `dispel` | 1 | 9.1% |
| `spawn` | 1 | 9.1% |
| `heal` | 1 | 9.1% |

**算子构成**（op，共 4 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 3 |
| `if` | 1 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `mount_status`(5)、`damage`(3)、`spawn`(1)；属性杠杆集中在 —，资源杠杆集中在 —；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **黄昏·万物崩解**：对一名敌人造成 4 点无元素伤害并施加「崩解」5 tick：每 tick 受 3 点无元素伤害并叠 1 层老化（最多 6 层），受伤 ×1.2，不可驱散。


## 附：统计口径

- 数据源：`docs/json/warrior.skills.json` 的 `cards[]`，共 33 张卡。
- 按 `axis` 字段分桶；`axis` 定义与符号取自文件级 `axes`。
- 效果原语/算子：**递归遍历**每张卡全部效果节点（卡面 `effects`、`statusDefs[].triggers[].effects`、区域/界域等任意嵌套），同名多次计数。
- 原语以效果节点 `type` 归类（19 类）；算子以 `op` 归类（9 类）。
- 挂载状态：统计每张卡 `statusDefs[]` 的 `category`（类型）与 `dispelable`（可驱散性）。
- 设计风格标签：由原语/属性/资源/状态类别映射到五大维度（进攻/防御/控制/运营/续航）后取占比≥25%的维度，至多 2 个组合而成。
- 生成脚本：`docs/tools/build_axis_analysis.py`（可重复运行）。