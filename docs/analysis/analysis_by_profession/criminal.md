# 罪犯（criminal）· 技能构筑轴分析

> 数据来源：`docs/json/criminal.skills.json`（共 **37** 张技能卡）。
> 由 `docs/tools/build_axis_analysis.py` 递归遍历全部效果节点，按「构筑轴(axis)」聚合生成。
> 生成日期：2026-09-23

## 一、职业总览：四条构筑轴一览

| 构筑轴 | 符号 | 名称 | 技能数 | 主动/被动 | 挂载状态 | 设计风格 |
|---|---|---|---:|---|---:|---|
| `vice` | 😈 | 恶欲 | 9 | 7/2 | 0 | 进攻 |
| `filth` | 🕳️ | 污秽 | 8 | 7/1 | 0 | 进攻 |
| `elements` | 🌋 | 领域 | 9 | 8/1 | 0 | 进攻 |
| `demon` | 👹 | 恶魔 | 11 | 4/7 | 0 | 防御 |
| **合计** | — | — | **37** | **26/11** | **0** | — |

### 1.1 职业级：挂载状态类型分布

> 全职业共挂载 **0** 个状态（可驱散 0 / 不可驱散 0）。
| 状态类型 | 次数 | 占状态% |
|---|---:|---:|

## 二、各构筑轴详解

### 😈 恶欲（vice）

**规模**：9 张技能（主动 7 / 被动 2）；稀有度分布：稀有×6、普通×2、传说×1。

**效果原语构成**（type，全量递归，共 19 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 12 | 63.2% |
| `damage` | 6 | 31.6% |
| `move` | 1 | 5.3% |

**算子构成**（op，共 7 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 4 |
| `if` | 2 |
| `push_back` | 1 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `mount_status`(12)、`damage`(6)、`move`(1)；属性杠杆集中在 —，资源杠杆集中在 —；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **堕落**：使一名近战敌人堕落 6 tick（不可驱散）：每次攻击落在自己身上，每 tick 受到 2 点无视一切防护的毒素伤害，受到的治疗减半。


### 🕳️ 污秽（filth）

**规模**：8 张技能（主动 7 / 被动 1）；稀有度分布：史诗×3、传说×3、普通×1、精良×1。

**效果原语构成**（type，全量递归，共 29 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 16 | 55.2% |
| `damage` | 9 | 31.0% |
| `modify_stat` | 3 | 10.3% |
| `heal` | 1 | 3.4% |

**算子构成**（op，共 7 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 1 |
| `if` | 6 |

**改动的属性（modify_stat，共 3 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `gauge.rate` | 3 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `mount_status`(16)、`damage`(9)、`modify_stat`(3)；属性杠杆集中在 `gauge.rate`，资源杠杆集中在 —；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **侵蚀者**：使一名敌人被侵蚀 5 tick：每 tick 受到 3 点无视护甲、护盾与抗性的毒素伤害，护甲 -2（5 tick）。可被驱散。


### 🌋 领域（elements）

**规模**：9 张技能（主动 8 / 被动 1）；稀有度分布：精良×3、稀有×3、普通×2、传说×1。

**效果原语构成**（type，全量递归，共 28 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 11 | 39.3% |
| `damage` | 13 | 46.4% |
| `modify_stat` | 2 | 7.1% |
| `modify_resource` | 1 | 3.6% |
| `domain` | 1 | 3.6% |

**算子构成**（op，共 3 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 2 |
| `overlay` | 1 |

**改动的属性（modify_stat，共 2 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `armor` | 2 |

**改动的资源（modify_resource，共 1 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `lost` | 1 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `damage`(13)、`mount_status`(11)、`modify_stat`(2)；属性杠杆集中在 `armor`，资源杠杆集中在 `lost`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **深渊之火**：全体敌人受到 5 点火焰伤害，护甲 -2（4 tick），中毒 3 tick 并叠加 1 层呓语（3 tick）。


### 👹 恶魔（demon）

**规模**：11 张技能（主动 4 / 被动 7）；稀有度分布：精良×5、史诗×3、普通×2、传说×1。

**效果原语构成**（type，全量递归，共 21 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 10 | 47.6% |
| `modify_stat` | 7 | 33.3% |
| `modify_resource` | 2 | 9.5% |
| `spawn` | 1 | 4.8% |
| `drain` | 1 | 4.8% |

**算子构成**（op，共 2 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 2 |

**改动的属性（modify_stat，共 7 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `hp_max` | 2 |
| `gauge.rate` | 2 |
| `armor` | 1 |
| `resist:mental` | 1 |
| `resist:physical` | 1 |

**改动的资源（modify_resource，共 2 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `hp` | 1 |
| `energy` | 1 |

**设计风格总结**：本轴偏【防御】——以叠甲、抬血上限与增益/架势类状态构筑防线，偏向站桩与减伤。
签名原语为 `mount_status`(10)、`modify_stat`(7)、`modify_resource`(2)；属性杠杆集中在 `hp_max`、`gauge.rate`、`armor`，资源杠杆集中在 `hp`、`energy`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **深渊即我**：战斗开始时获得「深渊即我」：火焰与毒素抗性各 +30%；自身施加的减益与持续伤害状态不可被驱散；对带减益或持续伤害状态的目标造成的伤害 ×1.3。


## 附：统计口径

- 数据源：`docs/json/criminal.skills.json` 的 `cards[]`，共 37 张卡。
- 按 `axis` 字段分桶；`axis` 定义与符号取自文件级 `axes`。
- 效果原语/算子：**递归遍历**每张卡全部效果节点（卡面 `effects`、`statusDefs[].triggers[].effects`、区域/界域等任意嵌套），同名多次计数。
- 原语以效果节点 `type` 归类（19 类）；算子以 `op` 归类（9 类）。
- 挂载状态：统计每张卡 `statusDefs[]` 的 `category`（类型）与 `dispelable`（可驱散性）。
- 设计风格标签：由原语/属性/资源/状态类别映射到五大维度（进攻/防御/控制/运营/续航）后取占比≥25%的维度，至多 2 个组合而成。
- 生成脚本：`docs/tools/build_axis_analysis.py`（可重复运行）。