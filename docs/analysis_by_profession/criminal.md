# 罪犯（criminal）· 技能构筑轴分析

> 数据来源：`docs/json/criminal.skills.json`（共 **37** 张技能卡）。
> 由 `docs/build_axis_analysis.py` 递归遍历全部效果节点，按「构筑轴(axis)」聚合生成。
> 生成日期：2026-09-17

## 一、职业总览：四条构筑轴一览

| 构筑轴 | 符号 | 名称 | 技能数 | 主动/被动 | 挂载状态 | 设计风格 |
|---|---|---|---:|---|---:|---|
| `vice` | 🜁 | 恶欲 | 9 | 7/2 | 6 | 进攻·控制 |
| `filth` | 🜂 | 污秽 | 8 | 7/1 | 6 | 进攻 |
| `elements` | 🜃 | 领域 | 9 | 8/1 | 2 | 进攻 |
| `demon` | 🜄 | 恶魔 | 11 | 4/7 | 8 | 防御 |
| **合计** | — | — | **37** | **26/11** | **22** | — |

### 1.1 职业级：挂载状态类型分布

> 全职业共挂载 **22** 个状态（可驱散 11 / 不可驱散 11）。
| 状态类型 | 次数 | 占状态% |
|---|---:|---:|
| buff | 11 | 50.0% |
| debuff | 8 | 36.4% |
| control | 3 | 13.6% |
| conceal | 1 | 4.5% |

## 二、各构筑轴详解

### 🜁 恶欲（vice）

**规模**：9 张技能（主动 7 / 被动 2）；稀有度分布：稀有×6、普通×2、传说×1。

**效果原语构成**（type，全量递归，共 25 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 13 | 52.0% |
| `damage` | 9 | 36.0% |
| `modify_stat` | 2 | 8.0% |
| `move` | 1 | 4.0% |

**算子构成**（op，共 9 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 4 |
| `if` | 2 |
| `target_override` | 2 |
| `push_back` | 1 |

**改动的属性（modify_stat，共 2 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `resist:mental` | 2 |

**挂载状态类型（共 6 个；可驱散 3 / 不可驱散 3）**：

| 状态类型 | 次数 | 占本轴状态% |
|---|---:|---:|
| buff | 2 | 33.3% |
| debuff | 2 | 33.3% |
| control | 2 | 33.3% |
| conceal | 1 | 16.7% |

**设计风格总结**：本轴偏【进攻·控制】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀；以 debuff、换位、封印、重定向与驱散干扰敌方决策与站位。
签名原语为 `mount_status`(13)、`damage`(9)、`modify_stat`(2)；属性杠杆集中在 `resist:mental`，资源杠杆集中在 —；挂载状态以 buff、control、debuff、conceal 类型为主，构成其核心交互骨架。
代表技能：
- **堕落**：使一名近战敌人堕落 6 tick（不可驱散）：每次攻击落在自己身上，每 tick 受到 2 点无视一切防护的毒素伤害，受到的治疗减半。


### 🜂 污秽（filth）

**规模**：8 张技能（主动 7 / 被动 1）；稀有度分布：史诗×3、传说×3、普通×1、精良×1。

**效果原语构成**（type，全量递归，共 32 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 14 | 43.8% |
| `damage` | 11 | 34.4% |
| `modify_stat` | 6 | 18.8% |
| `heal` | 1 | 3.1% |

**算子构成**（op，共 7 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 1 |
| `if` | 6 |

**改动的属性（modify_stat，共 6 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `gauge.rate` | 3 |
| `armor` | 1 |
| `attack` | 1 |
| `defense` | 1 |

**挂载状态类型（共 6 个；可驱散 5 / 不可驱散 1）**：

| 状态类型 | 次数 | 占本轴状态% |
|---|---:|---:|
| buff | 1 | 16.7% |
| debuff | 5 | 83.3% |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `mount_status`(14)、`damage`(11)、`modify_stat`(6)；属性杠杆集中在 `gauge.rate`、`armor`、`attack`，资源杠杆集中在 —；挂载状态以 debuff、buff 类型为主，构成其核心交互骨架。
代表技能：
- **侵蚀者**：使一名敌人被侵蚀 5 tick：每 tick 受到 3 点无视护甲、护盾与抗性的毒素伤害，护甲 -2（5 tick）。可被驱散。


### 🜃 领域（elements）

**规模**：9 张技能（主动 8 / 被动 1）；稀有度分布：精良×3、稀有×3、普通×2、传说×1。

**效果原语构成**（type，全量递归，共 28 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 11 | 39.3% |
| `damage` | 13 | 46.4% |
| `modify_stat` | 2 | 7.1% |
| `modify_resource` | 1 | 3.6% |
| `domain` | 1 | 3.6% |

**算子构成**（op，共 4 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 2 |
| `target_override` | 1 |
| `overlay` | 1 |

**改动的属性（modify_stat，共 2 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `armor` | 2 |

**改动的资源（modify_resource，共 1 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `lost` | 1 |

**挂载状态类型（共 2 个；可驱散 1 / 不可驱散 1）**：

| 状态类型 | 次数 | 占本轴状态% |
|---|---:|---:|
| buff | 1 | 50.0% |
| control | 1 | 50.0% |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `damage`(13)、`mount_status`(11)、`modify_stat`(2)；属性杠杆集中在 `armor`，资源杠杆集中在 `lost`；挂载状态以 buff、control 类型为主，构成其核心交互骨架。
代表技能：
- **深渊之火**：全体敌人受到 5 点火焰伤害，护甲 -2（4 tick），中毒 3 tick 并叠加 1 层呓语（3 tick）。


### 🜄 恶魔（demon）

**规模**：11 张技能（主动 4 / 被动 7）；稀有度分布：精良×5、史诗×3、普通×2、传说×1。

**效果原语构成**（type，全量递归，共 32 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 12 | 37.5% |
| `damage` | 1 | 3.1% |
| `modify_stat` | 14 | 43.8% |
| `modify_resource` | 2 | 6.2% |
| `spawn` | 2 | 6.2% |
| `drain` | 1 | 3.1% |

**算子构成**（op，共 2 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 2 |

**改动的属性（modify_stat，共 14 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `hp_max` | 3 |
| `gauge.rate` | 3 |
| `armor` | 2 |
| `resist:mental` | 2 |
| `attack` | 1 |
| `resist:physical` | 1 |
| `resist:fire` | 1 |
| `resist:poison` | 1 |

**改动的资源（modify_resource，共 2 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `hp` | 1 |
| `energy` | 1 |

**挂载状态类型（共 8 个；可驱散 2 / 不可驱散 6）**：

| 状态类型 | 次数 | 占本轴状态% |
|---|---:|---:|
| buff | 7 | 87.5% |
| debuff | 1 | 12.5% |

**设计风格总结**：本轴偏【防御】——以叠甲、抬血上限与增益/架势类状态构筑防线，偏向站桩与减伤。
签名原语为 `modify_stat`(14)、`mount_status`(12)、`modify_resource`(2)；属性杠杆集中在 `hp_max`、`gauge.rate`、`armor`，资源杠杆集中在 `hp`、`energy`；挂载状态以 buff、debuff 类型为主，构成其核心交互骨架。
代表技能：
- **深渊即我**：战斗开始时获得「深渊即我」：火焰与毒素抗性各 +30%；自身施加的减益与持续伤害状态不可被驱散；对带减益或持续伤害状态的目标造成的伤害 ×1.3。


## 附：统计口径

- 数据源：`docs/json/criminal.skills.json` 的 `cards[]`，共 37 张卡。
- 按 `axis` 字段分桶；`axis` 定义与符号取自文件级 `axes`。
- 效果原语/算子：**递归遍历**每张卡全部效果节点（卡面 `effects`、`statusDefs[].triggers[].effects`、区域/界域等任意嵌套），同名多次计数。
- 原语以效果节点 `type` 归类（19 类）；算子以 `op` 归类（9 类）。
- 挂载状态：统计每张卡 `statusDefs[]` 的 `category`（类型）与 `dispelable`（可驱散性）。
- 设计风格标签：由原语/属性/资源/状态类别映射到五大维度（进攻/防御/控制/运营/续航）后取占比≥25%的维度，至多 2 个组合而成。
- 生成脚本：`docs/build_axis_analysis.py`（可重复运行）。