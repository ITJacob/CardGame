# 窥秘人（pryer）· 技能构筑轴分析

> 数据来源：`docs/json/pryer.skills.json`（共 **38** 张技能卡）。
> 由 `docs/build_axis_analysis.py` 递归遍历全部效果节点，按「构筑轴(axis)」聚合生成。
> 生成日期：2026-09-17

## 一、职业总览：四条构筑轴一览

| 构筑轴 | 符号 | 名称 | 技能数 | 主动/被动 | 挂载状态 | 设计风格 |
|---|---|---|---:|---|---:|---|
| `scrying` | 🜁 | 窥秘 | 6 | 2/4 | 6 | 防御·控制 |
| `combat` | 🜂 | 格斗 | 5 | 3/2 | 0 | 进攻 |
| `scroll` | 🜃 | 卷轴法术 | 12 | 11/1 | 6 | 进攻·控制 |
| `astral` | 🜄 | 星象信息 | 15 | 12/3 | 6 | 进攻·控制 |
| **合计** | — | — | **38** | **28/10** | **18** | — |

### 1.1 职业级：挂载状态类型分布

> 全职业共挂载 **18** 个状态（可驱散 9 / 不可驱散 9）。
| 状态类型 | 次数 | 占状态% |
|---|---:|---:|
| buff | 11 | 61.1% |
| debuff | 4 | 22.2% |
| control | 1 | 5.6% |
| conceal | 2 | 11.1% |
| retarget | 1 | 5.6% |
| aura | 1 | 5.6% |

## 二、各构筑轴详解

### 🜁 窥秘（scrying）

**规模**：6 张技能（主动 2 / 被动 4）；稀有度分布：普通×2、传说×2、稀有×1、史诗×1。

**效果原语构成**（type，全量递归，共 10 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 8 | 80.0% |
| `modify_resource` | 2 | 20.0% |

**改动的资源（modify_resource，共 2 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `energy` | 2 |

**挂载状态类型（共 6 个；可驱散 1 / 不可驱散 5）**：

| 状态类型 | 次数 | 占本轴状态% |
|---|---:|---:|
| buff | 4 | 66.7% |
| debuff | 1 | 16.7% |
| conceal | 1 | 16.7% |

**设计风格总结**：本轴偏【防御·控制】——以叠甲、抬血上限与增益/架势类状态构筑防线，偏向站桩与减伤；以 debuff、换位、封印、重定向与驱散干扰敌方决策与站位。
签名原语为 `mount_status`(8)、`modify_resource`(2)；属性杠杆集中在 —，资源杠杆集中在 `energy`；挂载状态以 buff、conceal、debuff 类型为主，构成其核心交互骨架。
代表技能：
- **隐匿权柄**：战斗开始时获得「隐匿权柄」：敌方单体技能难以选中你、受伤 ×0.9；你每次造成伤害后显形 2 tick（期间上述效果失效）。


### 🜂 格斗（combat）

**规模**：5 张技能（主动 3 / 被动 2）；稀有度分布：普通×2、史诗×2、稀有×1。

**效果原语构成**（type，全量递归，共 16 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 2 | 12.5% |
| `damage` | 7 | 43.8% |
| `modify_stat` | 3 | 18.8% |
| `modify_resource` | 1 | 6.2% |
| `dispel` | 1 | 6.2% |
| `move` | 1 | 6.2% |
| `drain` | 1 | 6.2% |

**算子构成**（op，共 6 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 2 |
| `if` | 3 |
| `pull_forward` | 1 |

**改动的属性（modify_stat，共 3 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `hp_max` | 1 |
| `defense` | 1 |
| `armor` | 1 |

**改动的资源（modify_resource，共 1 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `gauge.current` | 1 |

**设计风格总结**：本轴偏【进攻】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀。
签名原语为 `damage`(7)、`modify_stat`(3)、`mount_status`(2)；属性杠杆集中在 `hp_max`、`defense`、`armor`，资源杠杆集中在 `gauge.current`；挂载状态以 — 类型为主，构成其核心交互骨架。
代表技能：
- **朗基努斯之枪**：对一名近战敌人造成 8 点物理伤害，驱散其 1 个增益状态，并使其护甲上限 -5（3 tick）。


### 🜃 卷轴法术（scroll）

**规模**：12 张技能（主动 11 / 被动 1）；稀有度分布：精良×8、稀有×3、普通×1。

**效果原语构成**（type，全量递归，共 40 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 14 | 35.0% |
| `damage` | 11 | 27.5% |
| `modify_stat` | 8 | 20.0% |
| `modify_resource` | 1 | 2.5% |
| `dispel` | 3 | 7.5% |
| `spawn` | 1 | 2.5% |
| `move` | 2 | 5.0% |

**算子构成**（op，共 11 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 2 |
| `if` | 5 |
| `repeat` | 1 |
| `target_override` | 1 |
| `push_back` | 1 |
| `pull_forward` | 1 |

**改动的属性（modify_stat，共 8 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `attack` | 2 |
| `defense` | 2 |
| `gauge.rate` | 2 |
| `energy_max` | 1 |
| `energy_regen` | 1 |

**改动的资源（modify_resource，共 1 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `gauge.current` | 1 |

**挂载状态类型（共 6 个；可驱散 5 / 不可驱散 1）**：

| 状态类型 | 次数 | 占本轴状态% |
|---|---:|---:|
| buff | 3 | 50.0% |
| debuff | 2 | 33.3% |
| control | 1 | 16.7% |
| retarget | 1 | 16.7% |

**设计风格总结**：本轴偏【进攻·控制】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀；以 debuff、换位、封印、重定向与驱散干扰敌方决策与站位。
签名原语为 `mount_status`(14)、`damage`(11)、`modify_stat`(8)；属性杠杆集中在 `attack`、`defense`、`gauge.rate`，资源杠杆集中在 `gauge.current`；挂载状态以 buff、debuff、control、retarget 类型为主，构成其核心交互骨架。
代表技能：
- **神秘再现**：若自身有法术学识层数：消耗全部层数 N，对目标造成 N 段各 3 点无元素伤害；若 N≥3 额外在己方队尾召唤卷轴造物并使自己隐秘知识 +1。无层数时退化为 3 点无元素伤害并使法术学识 +1。


### 🜄 星象信息（astral）

**规模**：15 张技能（主动 12 / 被动 3）；稀有度分布：稀有×6、史诗×5、传说×3、普通×1。

**效果原语构成**（type，全量递归，共 40 处）：

| 原语 | 次数 | 占本轴% |
|---|---:|---:|
| `mount_status` | 10 | 25.0% |
| `damage` | 7 | 17.5% |
| `modify_stat` | 5 | 12.5% |
| `modify_resource` | 8 | 20.0% |
| `dispel` | 4 | 10.0% |
| `move` | 2 | 5.0% |
| `domain` | 1 | 2.5% |
| `transfer_status` | 1 | 2.5% |
| `snapshot` | 1 | 2.5% |
| `restore_snapshot` | 1 | 2.5% |

**算子构成**（op，共 10 处）：

| 算子 | 次数 |
|---|---:|
| `sequence` | 5 |
| `if` | 2 |
| `push_back` | 1 |
| `overlay` | 1 |
| `insert_tail_cross_lane` | 1 |

**改动的属性（modify_stat，共 5 处）**：

| stat 属性键 | 次数 |
|---|---:|
| `gauge.rate` | 2 |
| `attack` | 2 |
| `energy_regen` | 1 |

**改动的资源（modify_resource，共 8 处）**：

| resource 资源键 | 次数 |
|---|---:|
| `gauge.current` | 7 |
| `lost` | 1 |

**挂载状态类型（共 6 个；可驱散 3 / 不可驱散 3）**：

| 状态类型 | 次数 | 占本轴状态% |
|---|---:|---:|
| buff | 4 | 66.7% |
| debuff | 1 | 16.7% |
| conceal | 1 | 16.7% |
| aura | 1 | 16.7% |

**设计风格总结**：本轴偏【进攻·控制】——以直接伤害/削血与伤害乘区为核心手段，强调输出节奏与斩杀；以 debuff、换位、封印、重定向与驱散干扰敌方决策与站位。
签名原语为 `mount_status`(10)、`modify_resource`(8)、`damage`(7)；属性杠杆集中在 `gauge.rate`、`attack`、`energy_regen`，资源杠杆集中在 `gauge.current`、`lost`；挂载状态以 buff、debuff、conceal、aura 类型为主，构成其核心交互骨架。
代表技能：
- **数字象征·零与一**：若自身生命低于 50%：全体友军获得 6 点护盾（4 tick）且攻击 +2（4 tick）（「一」·造物主）；否则：驱散全体敌人全部增益并造成 5 点无元素伤害（「零」·混沌）。


## 附：统计口径

- 数据源：`docs/json/pryer.skills.json` 的 `cards[]`，共 38 张卡。
- 按 `axis` 字段分桶；`axis` 定义与符号取自文件级 `axes`。
- 效果原语/算子：**递归遍历**每张卡全部效果节点（卡面 `effects`、`statusDefs[].triggers[].effects`、区域/界域等任意嵌套），同名多次计数。
- 原语以效果节点 `type` 归类（19 类）；算子以 `op` 归类（9 类）。
- 挂载状态：统计每张卡 `statusDefs[]` 的 `category`（类型）与 `dispelable`（可驱散性）。
- 设计风格标签：由原语/属性/资源/状态类别映射到五大维度（进攻/防御/控制/运营/续航）后取占比≥25%的维度，至多 2 个组合而成。
- 生成脚本：`docs/build_axis_analysis.py`（可重复运行）。