# 卡牌对战系统 · 总览与决策记录 v1.5



本文件是整套文档的入口。不含设计细节，只回答三件事：有哪些文档、拍过哪些板、接下来干什么。
更新日期：2026-08-30

||||
|---|---|---|
|00_总览与决策（本文）|文档地图 + 版本演进 + 决策记录 A1–A24 + 待办|现在到哪一步了？|
|01_概念核对_v0.2|概念清单、核心抽象、决策卡|我们要建哪些概念？|
|02_详细设计_v1.5|字段级契约、生命周期状态机、47 条不变量、聚合边界、核心流程序列|这些概念怎么精确咬合？|
|03_参数层_v1.5|枚举取值域、数值归属重定位、冲突清单、回填清单|具体填什么值？|
|04_术语表_v0.1|术语词汇表（技能设计的前置产物）|技能怎么写最短？|


原始素材：总览 v6、战场站位与目标计算、行为模块 v1.5、状态模块 v1.3
剥离原则：去掉示例、枚举取值、平衡数值与具体实例参数，只留架构骨架；被剥离的内容全部收容进 03_参数层，不丢失、不错位。
冲突处理：总览 v6 存在滞后，以其余三份文档为准（已固化为裁定总则 A15）。
章节引用约定：各篇内部 §x.y 交叉引用均指本篇内。
同步到 IMA 的方式：一律分传（单篇过大会被截断）。知识库中 02_详细设计 拆为 _1 / _2 / _3 / _4 四片（§0–§4 / §5–§7 / §8 / §9–§14），03_参数层 拆为 _1 / _2 两片（§0–§1 / §2–§4）；本地各篇仍是一个文件，分片只存在于知识库。

## 一、版本演进

|||
|---|---|
|v0.1|首抽：6 个限界上下文、概念清单、6 个核心抽象、A1–A6 待拍板、B1–B6 冲突|
|v0.2|状态拆分为「本体 + 参数包」并推广为通用 Definition + Grant 模式；A1–A6 展开为决策卡；B1 确定性铁律落地为随机源治理；发现溯源键应为状态实例 ID 而非类型 ID|
|v1.0|字段级契约落地；聚合边界定为「整场战斗一个聚合，根是 Combat」；不变量形式化（24 条）|
|v1.1|近战 / 远程（reach）与同步对撞；守卫姿态（RedirectRule 扩展 protectedSelector + sourceFilter）+ stance 状态分类；目标选择 Auto / Manual 与确定性兜底|
|v1.2|元素（Element）：六属性纯标签化 + DamageChain 新增抗性阶段；术语（Term）：编目期展开 + originTerm 溯源烙印|
|v1.3|12 项待拍板事项全部裁定（B3 / B7 / B8 / B9 / §3.2–§3.7 / §3.10 / A2′），新增决策记录 A12–A16；伤害乘区化定调，DEF 杠杆与再生数值待重新标定；不变量 24 → 40 条|
|v1.4|进入技能设计阶段：术语算子集扩为 Sequence / Repeat / If（跨效果传值做成独立 EffectDef）；抗性钳制 [-50%, +75%]、档位 10%；SkillDef 采模板优先 + 逃生舱；推进顺序为先术语词汇表、再设计技能。新增 A17–A20|
|v1.5|效果类型族审计：补 dispel（否则 dispelable 是死字段）、登记 drain（A17 欠账）；划清 heal/modify_resource（A22）与 modify_stat/mount_status（A21）；区域载荷清单化 + 陷阱定位（A23）；开放 Gauge 三维度并给击退找承载（A24）。修复 bug：带 duration 的 modify_stat 原先无人计时、永不回滚。类型 7 → 9；不变量 43 → 47（并修正长期误计：此前称 40 条，实为 43 条）|


## 二、拍板结果总表（12 项，全部已裁定）


原「待拍板项总表」已全部转化为裁定记录。架构层再无阻塞项——剩余工作全部是数值标定与逐条标注。

||||
|---|---|---|
|B3|laneRef: auto 的解析时点|I 节点之前，由「施法者上下文」预处理为具体值（same_lane / all_lanes）；流水线保持纯函数，可独立测试与缓存 → A13|流水线纯函数性|
|B7|近战对撞是否校验"目标够得着攻击者"|首批不校验，一律对撞；规则简单可预期，扩展点保留 → A16|近战对撞规则|
|B8|术语多段伤害：一个 Action 内多段 vs 每段一个 Action|一个 Action 内多段；只触发一次近战对撞 / 一次受击 / 一次反击；第一段打死后第二段打空（不重选，溢出重选留作可选开关） → A12|术语展开语义|
|B9|元素 / 伤害大类是否拆两层|不拆两层，结算只走 element 一层；damageCategory 保留为只读派生 → A16|元素模型|
|§3.2|scope: radius 语义未定义|从取值域删除，改用 adjacent + spread 组合（旋风斩按此重配） → A15|枚举取值域 + 技能重配|
|§3.3|中毒时长冲突（行为模块 vs 状态模块）|采状态模块 v1.3：defaultDuration = 5 → A15|状态表|
|§3.4|再生总回复量 6 vs 12|采状态模块 v1.3：defaultDuration = 6、总回复 12；⚠️ 原平衡结论失效，需重新验证 → A15|平衡数值|
|§3.5|易伤：线性减防 vs 乘区|采 v1.3 乘区 ×1.3 → A14|全部平衡数值|
|§3.6|护盾：护甲载体 vs 护盾池|采状态模块 v1.3：独立 shield 池，defaultDuration = 6 → A15|状态表 + 伤害链|
|§3.7|眩晕机制口径冲突|采状态模块 v1.3：defaultDuration = 5，语义为 disable（禁用行为，可清姿态） → A15|状态表 + 控制链|
|§3.10|抗性口径：线性减法 vs 乘区|百分比乘区，与易伤同区加算后统一乘一次，强制上下限钳制 → A14|全部平衡数值|
|A2′|眩晕语义改为"剥夺机会"|本版不改，标记为待重评项；等骨架落地后用新版验证脚本对比两种实现的 TTK 差异再定|低优先|


更早已拍板并固化：A1–A11（权责、命名、拆分、溯源、近战远程、目标选择、元素、术语）、B1（确定性铁律 + 随机源治理 R1–R6）。

### 由此新增的决策记录

|||
|---|---|
|A12|B8 裁定：术语多段 = 一个 Action 内多段，只撞一次、只触发一次受击 / 反击，第二段打空不重选|
|A13|B3 裁定：laneRef: auto 在 I 节点之前由施法者上下文预处理为具体值|
|A14|数值口径统一乘区化：易伤 ×1.3、抗性百分比乘区，同乘区加算后统一乘一次并钳制；DEF 杠杆需重新标定|
|A15|冲突裁定总则：总览 v6 与状态模块 v1.3 冲突一律采 v1.3；scope: radius 删除，改用 adjacent + spread|
|A16|B7 近战对撞首批不校验可达性（扩展点保留）；B9 元素 / 伤害大类不拆两层|
|A17|术语算子集 = Sequence / Repeat / If；If 条件复用 EffectCondition 谓词。跨效果传值类（吸血等）不进术语，做成独立 EffectDef(drain)|
|A18|抗性钳制 [-50%, +75%]，基准全 0，投放以 10% 为一档，单卡 ±10~±20%|
|A19|SkillDef 采模板优先 + 逃生舱：默认填 templateRef 只覆写数值；允许 templateRef 为空、自带完整行为定义|
|A20|推进顺序 = 先出术语词汇表，再设计技能。术语是技能设计的前置产物|
|A21|modify_stat vs mount_status|
|A22|heal vs modify_resource|
|A23|区域载荷与陷阱定位|
|A24|Gauge 可修改性与击退建模|


说明：A17–A20 是进入技能设计阶段后新浮现的决策点（原 12 项之外的增量）；A21–A24 是效果类型族审计中裁定的增量。八项均已裁定。

## 三、回填清单（摘要）

完整 31 项见 03_参数层 §4。本次拍板顺带完成 8 项，剩余多为逐条标注的体力活：

- 已随拍板完成：scope: radius 处置、中毒 / 再生 / 易伤 / 护盾 / 眩晕的 defaultDuration 与口径、抗性口径、B3 / B7 / B8 / B9
- 剩余：给每个攻击行为标 reach、每个伤害效果标 element、每个 Manual 规格补 fallbackSort、每个姿态状态标 stance
- 缺失即阻塞：仍有 7 个状态（反伤 / 沉默 / 嘲讽 / 耗能加重 / 免吟唱 / 守护 / 伤害转移）缺 defaultDuration，INV-C2 在补齐前无法成立
## 四、当前待办（按优先级）

- 【进行中】术语词汇表校准：v0.1 已出（10 个术语 + 防误用清单 + 占位符体系），需拿完整技能清单校准后进入技能设计（A20）。
- 【最高】数值重新标定：用验证脚本重扫 TTK，重定「防御 → 输出」杠杆与再生 12 点总回复的合理性，同步更新参数层 §2.6 与平衡文档。不定完这条，所有卡面数值都是悬空的。
- 参数层回填：逐条标注（reach / element / fallbackSort / stance + 7 个缺失 defaultDuration）。
- 验证契约：把 47 条不变量写成可执行断言，接入验证脚本；环路校验（INV-T2）与白名单校验（INV-T3）先在 Catalog 加载器里做。
- 事件回放格式：基于 02_详细设计 §10.5 定义事件流 schema，配合 seed 精确回放；originTerm 与 element 必须进入事件载荷。
- B4 回写：把「状态伤害不进通用乘区」与本次 12 项裁定同步回总览 v6。
- B3 落地：实现施法者上下文预处理步骤，从配置里找出所有 laneRef: auto 并验证解析结果。
- 【基本完成】文案渲染契约：占位符体系已定义于 04_术语表 §4（{count} / {ratio} / {threshold} / {duration} / {stacks} / {element} / {armor_delta} / {status_name}），共 8 个。
- 【低优先】A2′ 复评：眩晕语义改「剥夺机会」，用新版验证脚本对比 TTK 差异后再定。

### 已知缺口（技能设计会撞上）

|||
|---|---|
|burn（灼烧）状态不在现有 15 个状态表里|ignite 术语与你 3 个示例技能（火球术 / 旋风斩 / 烈焰风暴）全都依赖它，必须补|
|7 个状态缺 defaultDuration|反伤 / 沉默 / 嘲讽 / 耗能加重 / 免吟唱 / 守护 / 伤害转移。技能挂这些状态时 INV-C2 无法成立|
|Repeat 表达不了多段不同倍率|"打两下、第二下半伤"当前只能 Sequence 硬写。等第二个多段技能出现再决定是否加 decay|
|击退（knockback）无 effect 承载|✅ 已解决（A24）：modify_resource 开放 Gauge 三维度，击退 = modify_resource(gauge.current, -50/n)。残留：公式里 n 的含义待确认（清单第 31 项）|
