# 编队参数与取值域

本文件收容编队上下文的全部枚举取值域与数值。默认值与上下限只写在 Def，实例值只写在 Grant/params（INV-C2）。

## 一、枚举取值域

### 1.1 状态（Status）

| 字段 | 取值 | 归属 | 说明 |
|---|---|---|---|
| StatusDef.category | dot / hot / buff / debuff / control / reactive / aura / stance | StatusDef | hot/debuff 与 dot/buff 语义有重叠，建议精简；stance 受 INV-P5 硬控清除规则约束 |
| trigger.event | on_apply / on_tick / on_take_damage / on_deal_damage / on_death / on_spawn / on_turn_start / on_remove / on_battle_start / on_kill | StatusDef | on_remove 用于配合溯源撤销 |
| stackPolicy | refresh / reject / stack | StatusDef | 已定 refresh |
| modifierRevert | on_expire_or_dispel | 引擎常量 | 已定 |
| source_filter | melee / ranged / skill / … | TriggerDef | 触发来源过滤 |

### 1.2 攻击类型（reach，v1.1 新增）

| 字段 | 取值 | 归属 | 说明 |
|---|---|---|---|
| reach | melee / ranged / none | BehaviorTemplate | 攻击类行为的属性；与站位、与 targetSpec 正交（INV-P6）；非攻击类为 none |
| protectedSelector.kind | unit_ref / coordinate_relation | StatusDef.RedirectRule | unit_ref = 守护指定单位；coordinate_relation = 守卫姿态（同路且 index 更大者） |
| coordinate_relation.relation | same_lane_behind / … | 同上 | 取值域待扩，首批只需 same_lane_behind |
| sourceFilter | melee / ranged / skill / any | StatusDef.RedirectRule | 守卫姿态填 ranged；守护填 any |
| stance 清除规则 | 硬控（stun）清除全部 category == stance | 引擎常量 | 分类驱动，不在眩晕里枚举状态（INV-P5） |

### 1.3 元素抗性（逐元素）

| 字段 | 取值 | 归属 | 说明 |
|---|---|---|---|
| ResistanceSet 槽位 | fire / ice / poison / shock / mental / physical | BaseProfile + ModifierLayer | 逐元素一槽，走 sourceId 撤销；基线建议全 0 |
| 抗性在 DamageChain 的位置 | 减免型之后、乘区型之前 | 引擎常量 | 与"防御线性减伤"是两笔账，不合并 |
| EffectCondition 元素谓词 | ElementIs(e) / ElementIn([...]) | 共享内核 | 供状态触发与流派判定 |

## 二、数值清单

### 2.1 单位与派生

| 参数 | 值 | 原出处 | 归属 | 标记 |
|---|---|---|---|---|
| 生命上限 | 30 + 10×力量 | 总览 §2.2 | 派生公式 → 编目 | 沿用 |
| 能量恢复 | 0.2 + 0.2×智力 / tick | 总览 §2.2 | Pool.regen 公式 | 沿用 |
| 能量上限 | 50 | 总览 §2.2 | Pool.max | 沿用 |
| 精力恢复 | 15 + 8×敏捷 / tick | 总览 §2.2 | Gauge.rate 公式 | 沿用 |
| 精力阈值 | 75 | 总览 §2.2 | Gauge.threshold | 沿用 |
| 行动周期 | 75/(15+8×敏捷) | 总览 §1 | 派生（threshold / rate） | 沿用 |
| 四档恢复值 | 15 / 23 / 31 / 39 | 总览 §1 | 派生结果，非配置 | 沿用 |
| 普攻基础伤害 | 2 | 总览 §2.2 | BaseProfile | 沿用 |
| 防御 | 0（负值=易伤） | 总览 §2.2 | BaseProfile | 沿用 |
| 护甲 | 0 | 总览 §2.2 | Pool（吸收型） | 沿用 |
| 技能伤害基准 | 1.2 × 能量 | 总览 §1 | 参数层（平衡基准） | 沿用 |

重定位说明：attack / defense / armor 在新模型里分属不同层——attack 与 defense 是 BaseProfile 的派生属性；armor 是吸收型 Pool（DamageChain 的一个阶段），不再是面板标量。

### 2.2 状态数值（含默认与实例拆分）

按 StatusDef（默认与上限）与 StatusGrant（实例值）分开——这是 Definition + Grant 拆分后的归属。⚠️ 仍需补齐：反伤、沉默、嘲讽、耗能加重、免吟唱、守护、伤害转移 的 defaultDuration 尚未确定，否则 INV-C2（单点定义）无法成立。

| 状态 | StatusDef（默认/上限） | StatusGrant（实例值） | 标记 |
|---|---|---|---|
| 中毒 | defaultDuration: 5　maxStacks: 3　stackPolicy: refresh　dispelable: true | duration: 5　stacks: 1　value: 1 / tick（穿透护甲） | 已裁定 |
| 再生 | defaultDuration: 6　maxStacks: 1　dispelable: true | duration: 6　value: 2 / tick（总回复 12） | 已裁定；⚠️ 原平衡结论失效，需重验 |
| 反伤 | maxStacks: 1　dispelable: true　source_filter: melee | duration: 8　value: 1 | 沿用；缺 defaultDuration |
| 亡语 | duration: 0（一次性）　dispelable: false | 无（一次性） | 沿用 |
| 开场光环 | duration: 0（一次性）　dispelable: false | 无（一次性）　value: +2 护甲 | 沿用 |
| 易伤 | defaultDuration: 4　maxStacks: 1　dispelable: true | duration: 4　mul: 1.3 | 已裁定（乘区）；⚠️ DEF 杠杆需重新标定 |
| 坚守 | maxStacks: 1　dispelable: true | duration: 4　mul: 0.7 | 沿用 |
| 护盾 | defaultDuration: 6　maxStacks: 1　dispelable: true | duration: 6　delta: 5（独立 shield 池，先于 HP 扣减） | 已裁定 |
| 沉默 | maxStacks: 1　dispelable: true | duration: 8　disable: 使用技能 | 沿用；缺 defaultDuration |
| 嘲讽 | maxStacks: 1　dispelable: true | duration: 6　force: 普攻 + target_override | 沿用；缺 defaultDuration |
| 耗能加重 | maxStacks: 1　dispelable: true | duration: 10　energy_cost_mul: 1.5 | 沿用；缺 defaultDuration |
| 免吟唱 | maxStacks: 1　dispelable: true | duration: 5　cast_time_set: 0 | 沿用；缺 defaultDuration |
| 眩晕 | defaultDuration: 5　maxStacks: 1　dispelable: false | duration: 5　disable: 全部四个行为 | 已裁定；"剥夺机会"方案本版不改 |
| 守护 | maxStacks: 1　dispelable: true　priority: 10 | duration: 6　binding.guardTarget: 被保护单位 | 沿用；缺 defaultDuration |
| 伤害转移 | maxStacks: 1　dispelable: true　priority: 5 | duration: 5　to: attacker | 沿用；缺 defaultDuration |

### 2.3 元素抗性数值

| 参数 | 值 | 归属 | 标记 |
|---|---|---|---|
| 基础抗性 | 基线全 0 | BaseProfile | 已裁定 |
| 抗性投放档位 | 10% 为一档，单卡 ±10~±20% | 参数层 | 已裁定；与「防御 ±1 为档、护甲 5 为档」同构 |
| 抗性口径 | 百分比乘区 | 参数层 | 已裁定 |
| 抗性上下限 | 钳制 [-50%, +75%] | 引擎常量 | 已裁定：负抗最多增伤 50%（防爆发翻倍）；正抗最多减伤 75%（留一线） |
| 抗性与乘区关系 | 与易伤/坚守同乘区加算，统一乘一次 | 引擎常量 | 已裁定；避免乘区数量爆炸 |
| 抗性撤销 | 按 sourceId | StatProvenance | 复用既有机制，零新增逻辑 |

抗性不是特殊属性：它和攻击、防御走同一套三层模型与同一份溯源账本，只是按元素分槽。
